import * as NullaryCommand from 'src/TreeifyWindow/Internal/NullaryCommand/all'

/** パラメータを持たないコマンドをまとめる名前空間 */
export {NullaryCommand}
// TODO: 本来は次の形式で書くべきだが、WebStormが未対応らしく入力補完が効かなくなる
// export * as CurrentState from "src/TreeifyWindow/Internal/CurrentState/all";
