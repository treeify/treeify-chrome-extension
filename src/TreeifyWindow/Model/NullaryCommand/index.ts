import * as NullaryCommand from 'src/TreeifyWindow/Model/NullaryCommand/all'

/** パラメータを持たないコマンドをまとめる名前空間 */
export {NullaryCommand}
// TODO: 本来は次の形式で書くべきだが、WebStormが未対応らしく入力補完が効かなくなる
// export * as NextState from "src/TreeifyWindow/Model/NextState/all";
